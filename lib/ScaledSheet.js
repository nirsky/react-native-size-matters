import { StyleSheet } from 'react-native';
import { moderateScale, scale, verticalScale } from './scalingUtils';


const ScaledSheet = {
    create: (styleSheet, delimeter = '@') => {
        
    ;}
};


export default ScaledSheet;